import { signal } from "@preact/signals";

// TODO: Handle collections of subjects / steps as sessions
//       Event handling in ActionBar.js
//       Steps -> Subjects -> Sessions
//       Note: should not need to handle any hierarchy assignment,
//             already relational
//       Get requests to sessions should return ALL populated data
//       And handle to canvas
//       *
//       Need to integrate sessions into UI, either in ActionBar and
//          force subjects into Steps section, OR as new UI element

const createSessionState = () => {
}

export default createSessionState;